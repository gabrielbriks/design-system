import type { StoryObj, Meta } from '@storybook/react'
import { Button } from '@morais-ui/react'

export default {
  title: 'Button', // Titulo da pagina ou da story
  component: Button, // omponente lá do pacote react
} as Meta

